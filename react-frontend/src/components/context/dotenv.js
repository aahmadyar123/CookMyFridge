import dotenv from 'dotenv';

dotenv.config();

export default {
  //BACKEND_URL: process.env.REACT_APP_BACKEND_URL
  BACKEND_URL: "https://cookmyfridge-backend.azurewebsites.net"
  // Add other environment variables here
};
